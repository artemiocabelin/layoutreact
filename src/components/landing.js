import React, { Component } from 'react';

export default class Landing extends Component {
  render() {
    const iblock = {display: 'inline-block'};
    
    return (
      <div style={iblock}>Main Component</div>
    );
  }
}
